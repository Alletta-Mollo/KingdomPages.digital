import React from 'react';
declare const HoverItem: React.MemoExoticComponent<({ slide, totalPages, totalSlides }: {
    slide: number;
    totalPages: number;
    totalSlides: number;
}) => React.JSX.Element>;
export default HoverItem;
