import React from "react";
declare const Slider: ({ maxSlide, currentSlide, onSlideChange, totalPages }: {
    maxSlide: number;
    currentSlide: number;
    onSlideChange: any;
    totalPages: number;
}) => React.JSX.Element;
export default Slider;
